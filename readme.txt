Requirements:

Python 2.x
Numpy (pip install numpy)


Input:

In the input folder, put the Mass Spec data in a mass_spec.csv file and the Temperature data in a temperature.txt file

Run Process.py

The output will be placed as output.txt in the output folder.
